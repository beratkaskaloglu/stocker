markets:
  - US
  - BIST

timeframes:
  - 30m
  - 1h
  - 1d

tau_values:
  30m: [1, 2, 4, 8]
  1h:  [1, 4, 8]
  1d:  [1, 5, 20, 60]

feature_dim: 211
seq_len: 60
batch_size: 64

walk_forward:
  window_days: 252
  step_days: 21
  min_folds: 5

overfitting:
  dropout: 0.3
  weight_decay: 0.0001
  early_stopping_patience: 15
